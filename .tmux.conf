unbind C-b
set -g prefix `
bind-key ` last-window
bind-key e send-prefix # to type `


bind-key & kill-window # kill without prompt
bind-key x kill-pane # kill without prompt


# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
